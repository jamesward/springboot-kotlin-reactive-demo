rootProject.name = "springboot-kotlin-reactive-demo"

pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven(uri("https://repo.spring.io/release"))
    }
}
