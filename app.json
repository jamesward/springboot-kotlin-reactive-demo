{
  "options": {
    "memory": "1Gi",
    "cpu": "2"
  },
  "build": {
    "skip": true
  },
  "hooks": {
    "prebuild": {
      "commands": [
        "unset DOCKER_HOST; ./gradlew --console=plain bootBuildImage --imageName=$IMAGE_URL"
      ]
    }
  }
}
