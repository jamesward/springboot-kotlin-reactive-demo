{
  "options": {
    "cpu": "2",
    "memory": "1Gi"
  },
  "build": {
    "skip": true
  },
  "hooks": {
    "prebuild": {
      "commands": [
        "gradle --console=plain bootBuildImage --imageName=$IMAGE_URL"
      ]
    }
  }
}
