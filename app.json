{
  "options": {
    "memory": "1Gi",
    "cpu": "2"
  },
  "build": {
    "skip": true
  },
  "hooks": {
    "prebuild": {
      "commands": [
        "DOCKER_HOST=/var/run/docker.sock ./mvnw spring-boot:build-image -Dspring-boot.build-image.imageName=$IMAGE_URL"
      ]
    }
  }
}
