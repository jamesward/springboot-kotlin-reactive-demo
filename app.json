{
  "options": {
    "memory": "1Gi",
    "cpu": "2"
  },
  "build": {
    "skip": true
  },
  "hooks": {
    "prebuild": {
      "commands": [
        "DOCKER_HOST=/var/run/docker.sock ./gradlew --console=plain bootBuildImage --imageName=$IMAGE_URL"
      ]
    }
  }
}
