{
  "options": {
    "cpu": "2",
    "memory": "1Gi"
  },
  "build": {
    "skip": true
  },
  "hooks": {
    "prebuild": {
      "commands": [
        "./gradlew --console=plain bootBuildImage --imageName=$IMAGE_URL"
      ]
    }
  }
}
